---
description: 长篇小说项目的长期事实一致性与 Canon v3 真源保护；仅在书项目文件命中时加载。
globs: "**/.story-system/**,**/.canon-ledger/state.json,**/正文/**/*.md,**/大纲/**/*.md,**/设定集/**/*.md"
alwaysApply: false
---

# CanonLedger 长期事实边界

## 唯一事实权威

- 只有 `.story-system/v3/CURRENT` 可达的 Canon v3 HEAD，以及与该 HEAD 和 generation 精确绑定的 fresh projection，才是当前长期事实真源。
- `.canon-ledger/state.json`、`index.db`、旧报告、RAG、模型记忆和未发布 STAGING 都不能替代当前 HEAD。
- 写作、规划、审查、查询或恢复前，先读取 exact `canon-v3 status`；只执行同一份 workflow snapshot 返回的 `primary_action`，并使用对应 `/canon-ledger-*` Skill。

## 强制一致性范围

- 只检查后续章节必须依赖的客观长期事实：身份、时间线、永久状态、关系、知识边界、真实在场、物品持有、世界硬规则、承诺与开放问题。
- 已有正文或 active Canon 没有锚点的极低概率猜测可以忽略。
- 证据不足但可能影响长期事实时，交给人工确认；不得由模型猜测后直接发布。
- `fact-boundary/v2` 适用于章节候选、legacy 迁移、初始化设定和 author axioms。
  已知 advisory 不得生成 Canon effect；自由状态、关系或规则无法由闭合结构证明时，
  必须对 exact candidate/record 做人工分类，不能因字段名像“规则”或未命中软关键词
  就默认当作硬事实。旧 policy 的 active fact 未完成重认证前不得继续写作或事实查询。

## Advisory 范围

- 大纲、章纲、规划合同、未认证设定草案仅供创作参考，不是剧情法律，也不能因未履约而阻断事实事务。
- 文风、文笔、节奏、口吻、句式、审美、人物动机、性格模板、人设类型和一般因果永远属于可自定义偏好，不进入 Canon 强制检查。
- 设定草案中的客观硬事实，只有经过 author-axiom 的版本化提议、人工确认和发布后，才成为 active Canon。

## 权威存储保护

- 不直接编辑或删除 `.story-system/v3/` 的 CURRENT、对象、STAGING、决定、manifest 或 projection。
- 不把旧 Story System、兼容读模型或手工文件修改当作事实写入口；需要恢复时重新读取 workflow，并按其 `primary_action` 操作。
